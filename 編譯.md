---
title: 編譯

---

# 編譯
1. git clone git://github.com/osrg/ryu.git
2. cd ryu;git checkout v3.27;python ./setup.py install
3. 看 read me : https://github.com/muzixing/ryu/tree/master/ryu/app/network_awareness
4 .把整個資料夾複製到剛剛步驟2的對應資料夾
- sudo apt-get install python-pip
- sudo apt-get install python-setuptools
- sudo python ./setup.py install
- sudo pip install -r tools/pip-requires
5. 安裝測試 ryu-manager shortest_forwarding.py --observe-links --k-paths=2 --weight=bw
6. 學長的code: ryu-manager shortest_forwarding.py --observe-links --k-paths=2 --weight=hop --ofp-tcp-listen-port=6633
 
## 問題:
numpy: sudo apt install python-numpy
networkx: pip install networkx
'importlib_metadata>=1.7.0' 或是pip :pip install --upgrade "pip < 21.0"(因為2.7停止更新了所以用這東西)
> https://www.cjavapy.com/article/1605/?fbclid=IwAR1jhUcsu6XnuxQiHnZu35Em4y05jsdfIlKEGI0MpjJwT21qCDatSFw2tb0
---
# meter table
## 一定要
ovs-vsctl set bridge s1 datapath_type=netdev
ovs-vsctl set bridge s1 protocols=OpenFlow13

## 測試端直接寫死了

ovs-ofctl add-meter s1 meter=1,kbps,band=type=drop,rate=5000,type=drop,rate=12000 -O OpenFlow13
ovs-ofctl dump-meters s1 -O OpenFlow13

ovs-ofctl add-flow s1 in_port=1,action=meter:1,output:2 -O OpenFlow13
ovs-ofctl add-flow s1 in_port=2,action=output:1 -O OpenFlow13
## root xterm
ethtool -K enp0s3 tx off
## h1  xterm
ethtool -K h1-eth0 tx off

---
## iperf
iperf -c 10.0.0.4 -u -b 8M -t 20 -i 5 (h1)
iperf -c 10.0.0.3 -u -b 8M -t 20 -i 5
iperf -s -i 3 -u (h2)
## iperf-s
- iperf -c 10.0.0.4 -u -b 6M -i 1 -t 160 
- iperf -c 10.0.0.3 -u -b 8M -i 1 -t 150
- iperf -s -i 5 -u
- iperf -c 10.0.0.10 -u -b 8M -i 1 -t 1500

# communication
- proxy:{(Server_port,Client_port)}
-- python test_broker.py 9089 9090 
- Client:{(topic,address,port)}
-- python test_client.py -t  test  -a 127.0.0.1 -p 9090
- Server:{(topic,address,port)}
-- python test_pub.py -t test -a 127.0.0.1 -p 9089
# group table
- sudo ovs-ofctl -O OpenFlow13 dump-group-stats s1
 ovs-ofctl -O OpenFlow13 add-group s1 group_id=5,type=select,bucket=output:2,weight=70,bucket=output:1,weight=30
 ovs-ofctl -O OpenFlow13 add-flow s1 in_port=4,actions=group:5


https://github.com/saeenali/openvswitch/wiki/Stochastic-Switching-using-Open-vSwitch-in-Mininet

https://medium.com/@john.lin/%E7%B7%A8%E8%AD%AF-open-vswitch-v2-7-0-on-ubuntu-16-04-2-lts-6df0a2f7d648

# script

1. https://www.it145.com/9/12772.html
2. https://www.cnblogs.com/ownhp/p/9094908.html
iperf -u -s -i 15 



iperf -c 10.0.0.2 -u -b 8M -i 5 -t 100 
iperf -c 10.0.0.1 -u -b 7M -i 5 -t 90

iperf -c 10.0.0.6 -u -b 8M -i 5 -t 70
iperf -c 10.0.0.3 -u -b 7M -i 5 -t 165
iperf -c 10.0.0.4 -u -b 8M -i 5 -t 150
iperf -c 10.0.0.3 -u -b 7M -i 5 -t 140
iperf -c 10.0.0.1 -u -b 8M -i 5 -t 120
iperf -c 10.0.0.2 -u -b 7M -i 5 -t 160
iperf -c 10.0.0.5 -u -b 8M -i 5 -t 100
